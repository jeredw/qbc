export interface ProgramEntry {
  name: string;
  archivePath: string;
  title: string;
  author?: string;
  date: string;
  screenshot: string;
  tags: string[];
  specs: string;
  usage: string;
  notes: string;
}