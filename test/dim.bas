dim sigil$
redim shared ssh%
dim s as string
dim shared foo as string * 42 
redim q as double
dim x as single
dim lo as long
dim bar as sometype
dim the.thing as integer
redim shared q2 as single
dim foo$, bar%, baz as long, quux!
dim a, b, c
dim arq$(40)
dim ar(-2 to -1)
dim arz(0)
dim boo(k%)
dim multi(xxx, 0 to 20, 30 to 35, yyy), wowzas(hi, -5 to 30) as string
dim foo as string * maxsize%
