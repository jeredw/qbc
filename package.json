{
  "type": "module",
  "dependencies": {
    "antlr4ng": "^3.0.9"
  },
  "devDependencies": {
    "antlr4ng-cli": "^2.0.0",
    "esbuild": "^0.24.0",
    "esbuild-plugin-tsc": "^0.4.0"
  },
  "scripts": {
    "build": "esbuild build/Shell.ts --bundle --sourcemap --outfile=build/out.js"
  }
}
